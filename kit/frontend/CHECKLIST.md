# ✅ frontend/CHECKLIST — Master-Board acceptance

Every box true for the pages built.

## Structure
- ☐ File `<App> — Master Board`; a **page index** in Page 1's cover; each page has a cover/legend.
- ☐ Each page carved into named **Figma Sections** (navigable from layers + minimap).

## Foundations (P1)
- ☐ Colors are real **Variables**; swatches labeled name·hex·usage; fixed/functional colors marked.
- ☐ Type = real **text styles** with real specimen copy; spacing/radius/overlay + a **motion spec card** present.
- ☐ Rendered theme matches `{{THEME_MODE}}`.

## Assets (P1)
- ☐ Every asset is the **real uploaded file** (no redraws); mascot states captioned with their app-state.

## Components (P1)
- ☐ **Every** component in the components dir exists as a real **component with variants** for its states.

## App Map · Wireframes · Screens (P2)
- ☐ App map grouped by the real nav model (+ entry/auth).
- ☐ One **low-fi grey** wireframe per screen; visually distinct from hi-fi.
- ☐ Hi-fi screens at device width, **full length (uncropped)**, grouped by area.
- ☐ Every screen present — real or **labeled `— PLACEHOLDER`**.
- ☐ Each screen notes the **contract data it consumes** (or "none"); no screen invents data.
- ☐ Superseded designs (if kept) under **one** labeled `Alternates` section.

## Flows / Store / Brand (P3–5, if built)
- ☐ Each journey is a thumbnail strip with arrows + decision points.
- ☐ Store shots pair device mockup + headline per hero moment.
- ☐ Wordmark lockups (light+dark) · voice with **real copy per state** · broken-conventions · icon spec.

## Craft & honesty (hard gates)
- ☐ Everything on the **8-pt grid**; auto-layout; naming convention throughout.
- ☐ **No invented data** anywhere; estimates **labeled**; every token/screen traces to code or the contract.
- ☐ Board matches the running app **1:1** (reconciled, not rebuilt from memory).
