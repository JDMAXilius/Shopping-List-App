# The final screens — Bagged v1

All 29 frames of the settled product (28 surfaces + `01b` at 40 items), exported from the
canonical Figma page `138:978 · Bagged · Screens app current`. These ARE the result — the
post-cut scope, three tabs, one style.

**One style for the entire app** (`PRODUCT.md` §2): no light/dark variants. Warm paper,
persimmon action, mono prices, line-icon glyphs on aisle tints.

Numbering matches `design/references/` — the top-3 shipped-app references for screen `NN`
live in `design/references/NN-slug/`.
